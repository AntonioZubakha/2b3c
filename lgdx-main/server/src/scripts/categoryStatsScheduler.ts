import { logger } from '../utils/logger';

// Category stats scheduler - placeholder for future category statistics
// This scheduler was moved to market-price-calculator-service
export function initCategoryStatsScheduler() {
  logger.info('[CategoryStatsScheduler] Category statistics moved to market-price-calculator-service');
}

// Export for backward compatibility
export default initCategoryStatsScheduler;
