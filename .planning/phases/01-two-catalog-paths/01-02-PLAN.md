---
phase: 01-two-catalog-paths
plan: 02
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - apps/frontend/src/components/Navbar.tsx
  - apps/frontend/src/pages/CraftPage.tsx
  - apps/frontend/src/pages/CartPage.tsx
  - apps/frontend/src/i18n/locales/en/cart.ts
  - apps/frontend/src/i18n/locales/ru/cart.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
must_haves:
  truths:
    - "Buyer navbar exposes both atelier and ready-jewelry collections (per GAP-NAV-01, CONTEXT idea)"
    - "Craft bespoke completion adds to cart then continues via /cart (and /checkout), not a second checkout client inside CraftPage (per GAP-CRAFT-01)"
    - "Bespoke summary total shown to the user matches the sum that validate-prices will use (diamond + setting) unless backend fees are added in the same change"
    - "Cart recommendations run only for a real jewelry SKU derived from a jewelry line, never for bespoke synthetic IDs (GAP-CART-01)"
  artifacts:
    - path: "apps/frontend/src/components/Navbar.tsx"
      provides: "Buyer collections link"
    - path: "apps/frontend/src/pages/CraftPage.tsx"
      provides: "Cart-first bespoke flow + aligned totals"
    - path: "apps/frontend/src/pages/CartPage.tsx"
      provides: "Safe rec query + collections empty-state CTA"
  key_links:
    - from: "CraftPage StepSummary"
      to: "/cart"
      via: "navigate after POST add"
      pattern: "navigate\\('/cart'\\)"
---

<objective>
Close implementation gaps **GAP-NAV-01**, **GAP-CRAFT-01**, **GAP-CART-01**, and **GAP-CART-02** from `01-01-PLAN.md` using **frontend-only** changes unless you explicitly extend pricing-service (out of default scope). Honor locked decisions **D-01** (jewelry add payload unchanged), **D-02** (craft compatibility unchanged), **D-03** (invalidate `orderQueryKeys.carts()` after add — keep or add calls as needed). Purpose: align buyer UX with two catalog paths and a single post-cart checkout surface. Output: updated Navbar, CraftPage, CartPage, and minimal i18n for new cart copy.
</objective>

<context>
@.planning/phases/01-two-catalog-paths/01-01-PLAN.md
@apps/frontend/src/components/Navbar.tsx
@apps/frontend/src/pages/CraftPage.tsx
@apps/frontend/src/pages/CartPage.tsx
@apps/frontend/src/pages/JewelryPage.tsx
@apps/frontend/src/pages/CheckoutPage.tsx
@apps/frontend/src/pages/AuthPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buyer navbar — collections entry (GAP-NAV-01)</name>
  <files>apps/frontend/src/components/Navbar.tsx</files>
  <action>When `getUiSurface()` is `buyer`, extend the `links` array so it includes both `/craft` (atelier) and `/collections` using the same label key pattern as staff (`t('nav.collections')`) or an existing shared key. Preserve order: lead with atelier if that is current product emphasis, but collections must be visible without opening the hamburger on desktop layouts that show primary links. Do not add collections to `supplier` surface.</action>
  <verify>
    <automated>pnpm --filter frontend run lint</automated>
  </verify>
  <acceptance_criteria>Buyer surface menu includes `/collections` and `/craft`; supplier menu unchanged.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Craft bespoke — cart-first flow and honest totals (GAP-CRAFT-01)</name>
  <files>apps/frontend/src/pages/CraftPage.tsx</files>
  <action>After successful `POST …/cart/:sessionId/add` for bespoke: always call `invalidateQueries` for `orderQueryKeys.carts()` as today. Remove the immediate `POST …/checkout` branch and the shipping address form from this step — checkout belongs on `CheckoutPage` per unified post-catalog contour. Navigate: if no JWT, `navigate('/auth?redirect=' + encodeURIComponent('/cart'))` (fix bare `redirect=craft` string that is not a route). If JWT present, `navigate('/cart')`. Delete dead state: `orderId` success UI tied to inline checkout, `CheckoutResponse` import if unused, address `useState` and `formValid` tied only to removed checkout, and any now-unused helpers. Align the right-hand bill with authoritative bespoke pricing: **subtotal** must equal `setting.price + diamond.price` (same components `pricing-service` sums for `type === 'bespoke'`). Remove or relabel UI-only `craftsmanship` and `insurance` lines so the prominent total is not higher than the server-validated checkout total (prefer removal over inventing new server fees in this phase). Keep stepper and piece cards; summary step remains a review + single primary CTA that completes add-to-cart (reuse or rename button copy via i18n so it reads as add to bag / continue to cart, not pay now if misleading).</action>
  <verify>
    <automated>pnpm --filter frontend run lint</automated>
  </verify>
  <acceptance_criteria>No POST to the order checkout route remains in CraftPage; unauthenticated users are sent through auth toward `/cart`; displayed bespoke total matches diamond+setting sum.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Cart — recommendations guard and empty-state path (GAP-CART-01, GAP-CART-02)</name>
  <files>apps/frontend/src/pages/CartPage.tsx, apps/frontend/src/i18n/locales/en/cart.ts, apps/frontend/src/i18n/locales/ru/cart.ts</files>
  <action>Replace `firstSku = items[0]?.productId` with a derived SKU: walk `cart.items` and pick the first item where `type === 'jewelry'` and `productId` does not start with `BESPOKE-`; pass that to `recQuery` `enabled` and URL encoding. If no qualifying jewelry line, keep `enabled: false`. In the empty cart block, add a secondary CTA link to `/collections` beside or below the existing `/marketplace` link; add i18n keys in both locale files following existing `cart.*` naming.</action>
  <verify>
    <automated>pnpm --filter frontend run lint</automated>
  </verify>
  <acceptance_criteria>With only bespoke items in cart, recommendations query does not run; empty cart shows a collections link with translated label.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Browser → order cart add | Still accepts client `price`; unchanged in this plan. |
| Browser → checkout | Continues to rely on gateway auth + pricing validation server-side. |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | Client `price` on bespoke add | mitigate (existing) | Unchanged server validation at checkout; UI no longer shows extra fees that are not validated. |
| T-02-02 | I | Duplicate checkout surfaces | mitigate | Remove CraftPage inline checkout; single `CheckoutPage` path after cart. |
| T-02-03 | E | Bad recommendation SKU | mitigate | Jewelry-only SKU selection for `match/:sku` per Task 3. |
| T-02-04 | I | Open redirect via `redirect` query | mitigate | Use `encodeURIComponent('/cart')` only; do not pass arbitrary external URLs from craft without validating prefix (keep path internal). |
</threat_model>

<verification>
From repo root: `pnpm --filter frontend run lint` must pass after all tasks. Manually smoke: add bespoke as guest → login → lands on `/cart` with line present; add jewelry from `/collections` → `/cart` shows jewelry line; cart with bespoke only → no failed recommendations network call in devtools.
</verification>

<success_criteria>
- GAP-NAV-01, GAP-CRAFT-01, GAP-CART-01, GAP-CART-02 are resolved in code with no new order-service or gateway routes required for the default approach.
- D-01/D-02/D-03 behaviors preserved except intentional removal of inline checkout from craft (still uses same add payload shape: `type: 'bespoke'`, `bespokePair`, `productId`, `price`, `quantity`).
</success_criteria>

<output>
After execution, create `.planning/phases/01-two-catalog-paths/01-02-SUMMARY.md` if the orchestrator requires phase summaries.
</output>
