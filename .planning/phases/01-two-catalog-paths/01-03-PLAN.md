---
phase: 01-two-catalog-paths
plan: 03
type: execute
wave: 3
depends_on:
  - 2
files_modified:
  - apps/frontend/src/lib/cartRecommendationsSku.ts
  - apps/frontend/src/lib/cartRecommendationsSku.test.ts
  - info/FRONTEND.md
  - info/PRODUCT.md
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
must_haves:
  truths:
    - "Unit test covers jewelry SKU selection for recommendations helper"
    - "info/FRONTEND.md and info/PRODUCT.md describe two buyer paths and cart-first craft after plan 02"
    - "pnpm run lint and frontend vitest pass from monorepo root"
  artifacts:
    - path: "apps/frontend/src/lib/cartRecommendationsSku.ts"
      provides: "Pure helper for recommendation SKU (used or mirrored by CartPage)"
    - path: "apps/frontend/src/lib/cartRecommendationsSku.test.ts"
      provides: "Regression tests"
    - path: "info/FRONTEND.md"
      provides: "Route and flow documentation sync"
    - path: "info/PRODUCT.md"
      provides: "Product boundary documentation sync"
  key_links:
    - from: "cartRecommendationsSku"
      to: "CartPage recQuery"
      via: "import or inline-consistent logic"
      pattern: "pickJewelryRecommendationSku|cartRecommendationsSku"
---

<objective>
Regression and documentation pass after catalog-path implementation: extract or verify the **jewelry-only recommendation SKU** rule with a fast unit test, run repo lint and frontend tests, and sync **info/FRONTEND.md** and **info/PRODUCT.md** with the two-path model and post-02 flows (cart-first craft, buyer navbar collections). Purpose: lock behavior against future drift (QLT-style gate within phase 1). Output: helper + test (if not redundant after 02), updated info docs, green lint/tests.
</objective>

<context>
@.planning/phases/01-two-catalog-paths/01-02-PLAN.md
@info/FRONTEND.md
@info/PRODUCT.md
@apps/frontend/src/pages/CartPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit-test recommendation SKU selection</name>
  <files>apps/frontend/src/lib/cartRecommendationsSku.ts, apps/frontend/src/lib/cartRecommendationsSku.test.ts, apps/frontend/src/pages/CartPage.tsx</files>
  <action>If `CartPage` already implements jewelry-first SKU selection inline in plan 02, extract that logic into `pickJewelryRecommendationSku(items: Pick<CartItem,'type'|'productId'>[]): string | undefined` (adjust types to project contracts) in `cartRecommendationsSku.ts` and import it from `CartPage` to avoid drift. Tests must cover: empty cart → undefined; bespoke-only lines → undefined; first line bespoke then jewelry → returns jewelry SKU; jewelry with `productId` starting `BESPOKE-` should be treated as invalid for recommendations (return undefined or skip to next jewelry). Use Vitest like existing `src/lib/*.test.ts` files.</action>
  <verify>
    <automated>pnpm --filter frontend exec vitest run src/lib/cartRecommendationsSku.test.ts</automated>
  </verify>
  <acceptance_criteria>All tests in that file pass; CartPage uses the shared helper if the file exists.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repo lint (frontend packages touched)</name>
  <files>package.json, apps/frontend/**</files>
  <action>From monorepo root `E:\Stonee`, run `pnpm run lint` as documented in `PROJECT.md`. Fix any issues introduced by phase work only; do not widen eslint scope beyond what the command already checks.</action>
  <verify>
    <automated>pnpm run lint</automated>
  </verify>
  <acceptance_criteria>Lint exits zero.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Sync product and frontend info docs</name>
  <files>info/FRONTEND.md, info/PRODUCT.md</files>
  <action>Update **info/PRODUCT.md** “Два сценария” subsection to state explicitly: (1) ready jewelry primary entry `/collections`; (2) setting+diamond flow `/craft`; (3) both converge on **shared** `/cart` then `/checkout` after phase 02 implementation (no duplicate checkout inside craft). Update **info/FRONTEND.md** Navbar section: document buyer navbar now includes collections link; document craft summary behavior post-change (add to cart → `/cart` or auth with redirect to `/cart`). Keep paths as full paths in prose. Date footers optional; do not add new markdown files beyond these two.</action>
  <verify>
    <automated>pnpm --filter frontend exec vitest run</automated>
  </verify>
  <acceptance_criteria>Both files mention `/collections`, `/craft`, `/cart`, and cart-first bespoke; no stale claim that craft runs full checkout on the same page.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| CI / local test runner | Trusted; tests must not embed secrets. |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | Tests out of sync with CartPage | mitigate | Shared helper imported by CartPage and tested. |
| T-03-02 | I | Docs promise checkout behavior that code does not do | mitigate | Docs updated to match post-02 implementation; TRK-style mismatch avoided early. |
</threat_model>

<verification>
Run order: Task 1 vitest file → Task 2 `pnpm run lint` → Task 3 full frontend vitest. Confirm `info/FRONTEND.md` route table still matches `App.tsx` for all buyer routes touched this phase.
</verification>

<success_criteria>
- `pnpm run lint` and `pnpm --filter frontend exec vitest run` succeed from repo root.
- `info/PRODUCT.md` and `info/FRONTEND.md` reflect two catalog paths and unified cart/checkout wording consistent with REQUIREMENTS CAT-03 and ROADMAP phase 1 goal.
</success_criteria>

<output>
Create `.planning/phases/01-two-catalog-paths/01-03-SUMMARY.md` if the orchestrator requires it.
</output>
