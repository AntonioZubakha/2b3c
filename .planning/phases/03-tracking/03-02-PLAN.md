---
phase: 03-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - apps/frontend/src/lib/orderTracking.ts
  - apps/frontend/src/lib/orderTracking.test.ts
  - apps/frontend/src/pages/OrderDetailPage.tsx
  - apps/frontend/src/pages/OrdersPage.tsx
  - apps/frontend/src/i18n/locales/en/orders.ts
  - apps/frontend/src/i18n/locales/en/orderDetail.ts
  - apps/frontend/src/i18n/locales/ru/orders.ts
  - apps/frontend/src/i18n/locales/ru/orderDetail.ts
  - info/FRONTEND.md
  - info/API.md
  - scripts/smoke.mjs
autonomous: true
requirements:
  - TRK-01
  - TRK-02
must_haves:
  truths:
    - "На детали заказа видна линейка шагов, отмечающая только OrderStatus из API; текущий шаг совпадает с order.status"
    - "Подсказки по «изготовление vs отгрузка» не утверждают подэтапов, которых нет в status (per D-03-02)"
    - "Статический SLA-текст не противоречит статусу или заменён на условный/нейтральный"
    - "Smoke проверяет ожидаемый status после оплаты там, где выполняется buyer flow"
  artifacts:
    - path: "apps/frontend/src/lib/orderTracking.ts"
      provides: "Чистая логика completed/current для шагов timeline"
      min_lines: 20
    - path: "apps/frontend/src/lib/orderTracking.test.ts"
      provides: "Vitest на ключевые статусы и CANCELLED"
      min_lines: 15
    - path: "apps/frontend/src/pages/OrderDetailPage.tsx"
      provides: "Отображение timeline + условный копирайт"
      min_lines: 10
  key_links:
    - from: "OrderDetailPage.tsx"
      to: "order.status"
      via: "GET my-orders/:id response"
      pattern: "order\\.status"
---

<objective>
Закрыть **GAP-TRK-01**, **GAP-TRK-02a/b**, **GAP-SMK-01** из `03-01-PLAN.md`: минимальная **правдивая** линейка статусов на детали заказа, согласованный копирайт (ru/en), обновление `info/FRONTEND.md` и `info/API.md`, assert в `smoke.mjs` на фактический статус после оплаты. Purpose: TRK-01/02 без новых полей Mongo и без новых значений enum. Output: рабочий UI + тесты + доки + smoke.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tracking/03-CONTEXT.md
@.planning/phases/03-tracking/03-RESEARCH.md
@.planning/phases/03-tracking/03-01-PLAN.md
@apps/frontend/src/lib/contracts.ts
@apps/frontend/src/pages/OrderDetailPage.tsx
@apps/frontend/src/pages/OrdersPage.tsx
@info/FRONTEND.md
@info/API.md
@scripts/smoke.mjs
</context>

<interfaces>
From `apps/frontend/src/lib/contracts.ts`:
```typescript
export type OrderStatus = 'PENDING' | 'PAID' | 'CONFIRMED' | 'SHIPPED' | 'DELIVERED' | 'CANCELLED';
export type OrderItem = {
  type: 'diamond' | 'setting' | 'jewelry' | 'bespoke';
  productId: string;
  bespokePair?: { diamondId?: string; settingId?: string };
  price: number;
  quantity: number;
};
export type OrderDoc = ApiDoc<{
  userId: string;
  items: OrderItem[];
  totalAmount: number;
  status: OrderStatus;
  shippingAddress: { fullName: string; addressLine1: string; city: string; country: string; zipCode: string };
  paymentIntentId?: string;
  createdAt: string;
  updatedAt: string;
}>;
```
Использовать `OrderStatus` и `OrderItem['type']` как входные типы для хелперов трекинга.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Логика шагов timeline (TDD)</name>
  <files>apps/frontend/src/lib/orderTracking.ts, apps/frontend/src/lib/orderTracking.test.ts</files>
  <behavior>
    - Идентификаторы шагов в линейке — **только** литералы из `OrderStatus` (per D-03-01); без синтетических id вроде `PAYMENT_PENDING`.
    - Для `PENDING` текущий шаг — `PENDING`; завершённые шаги до него отсутствуют.
    - Для `CONFIRMED` и `PAID` — шаги до текущего отмечены completed согласно одному фиксированному порядку: `PENDING` → `PAID`/`CONFIRMED` → `SHIPPED` → `DELIVERED` (ветвление `PAID` vs `CONFIRMED` только для визуального текущего узла, без лишних «оплачено дважды»).
    - `CANCELLED` — отдельная ветка: не отображать `DELIVERED` как достигнутый; терминал отмены приоритетен.
    - Порядок узлов в массиве фиксирован и перечисляет только значения из `OrderStatus`.
  </behavior>
  <action>Добавить модуль `orderTracking.ts` с экспортом функций (например список шагов с полями `id: OrderStatus`, `completed`, `current`) — имена и сигнатуры на усмотрение исполнителя при сохранении контракта: **вход** `status: OrderStatus`, **выход** данные для рендера без fetch. Написать `orderTracking.test.ts` (Vitest) под `<behavior>`. Реализовать минимальный код до зелёных тестов. Не импортировать React в `orderTracking.ts`.</action>
  <verify>
    <automated>pnpm --filter frontend exec vitest run src/lib/orderTracking.test.ts</automated>
  </verify>
  <done>Тесты зелёные; функции покрывают PENDING, CONFIRMED, SHIPPED, DELIVERED, CANCELLED и ветку PAID согласованно с `OrderDetailPage` в Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: UI детали и списка + i18n</name>
  <files>apps/frontend/src/pages/OrderDetailPage.tsx, apps/frontend/src/pages/OrdersPage.tsx, apps/frontend/src/i18n/locales/en/orders.ts, apps/frontend/src/i18n/locales/en/orderDetail.ts, apps/frontend/src/i18n/locales/ru/orders.ts, apps/frontend/src/i18n/locales/ru/orderDetail.ts</files>
  <action>Встроить на `OrderDetailPage` визуальную линейку (достаточно доступной вёрстки в стиле существующих glass-card) на основе данных из Task 1 и `order.status`. Добавить **один** блок контекстной подсказки по `order.items` (например: если есть `bespoke` — текст про изготовление и сборку **как ожидаемый процесс**, явно не названный отдельным статусом API; если только `diamond`/`jewelry` без bespoke — акцент на подтверждении и логистике per D-03-02). Убрать или сделать **условными** ключи `orders.processingTime` и `orderDetail.processingNote` так, чтобы не утверждать фиксированный SLA при `SHIPPED`/`DELIVERED`/`CANCELLED` (per GAP-TRK-02a). Список заказов: при необходимости одна строка под бейджем с нейтральной подсказкой или ссылкой на детали — без дублирования длинной линейки. Все новые строки — в en и ru.</action>
  <verify>
    <automated>pnpm --filter frontend exec tsc --noEmit</automated>
  </verify>
  <done>Деталь заказа показывает timeline и согласованный копирайт; нет утверждений о подэтапах вне enum.</done>
</task>

<task type="auto">
  <name>Task 3: Документация и smoke</name>
  <files>info/FRONTEND.md, info/API.md, scripts/smoke.mjs</files>
  <action>Обновить `info/FRONTEND.md`: кратко описать buyer timeline (только OrderStatus), ограничение «нет manufacturing field», ссылка на `orderTracking.ts`. Обновить `info/API.md`: покупательский GET возвращает тот же `status`, что пишет машина состояний; напоминание про PATCH для SHIPPED/DELIVERED. В `smoke.mjs` после успешного `pay order` выполнить `GET /api/order/my-orders/:id` или найти заказ в списке и assert `status === 'CONFIRMED'`; для bespoke ветки — то же если ветка выполняется. Не менять compose и не добавлять новые сервисные эндпоинты.</action>
  <verify>
    <automated>pnpm run lint</automated>
  </verify>
  <done>Доки отражают UI; smoke содержит assert статуса после оплаты в выполняемых ветках.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Покупательский UI | Отображает только серверный `order.status`; timeline не добавляет скрытых «готово» без статуса. |
| Копирайт | Не заменяет юридические SLA; избегает обещания конкретных сроков без данных. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information | Пользователь видит «доставлено» из UI при `CONFIRMED` на сервере | mitigate | Current step строго из `order.status`; визуальные completed-флаги из функции Task 1 с тестами. |
| T-03-02-02 | Repudiation | Несоответствие скриншота UI и ответа API | mitigate | Ключи i18n описывают enum; FRONTEND.md фиксирует модель; smoke фиксирует `CONFIRMED` после pay. |
| T-03-02-03 | Tampering | XSS через адрес | accept | Без изменения рендера адреса вне scope; не вставлять сырой HTML. |
</threat_model>

<verification>
`pnpm --filter frontend exec vitest run src/lib/orderTracking.test.ts` и `pnpm run lint` зелёные; при доступном стеке `pnpm run smoke` с `BASE_URL` по умолчанию.
</verification>

<success_criteria>
- TRK-01: покупатель видит понятную последовательность, отражающую реальные статусы API.
- TRK-02: копирайт и подсказки не противоречат бэкенду; задокументированы ограничения.
- ROADMAP Phase 3 success #2: smoke обновлён под проверку статуса после оплаты.
</success_criteria>

<output>
После выполнения создать `.planning/phases/03-tracking/03-02-SUMMARY.md`.
</output>
